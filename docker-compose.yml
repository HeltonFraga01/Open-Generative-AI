version: '3.8'

services:
  comfyui:
    image: ghcr.io/ai-dock/comfyui:cpu-22.04
    environment:
      - COMFYUI_FLAGS=--cpu
      - WEB_ENABLE_AUTH=false
    ports:
      - "8188:8188"
    volumes:
      - comfyui_workspace:/workspace

volumes:
  comfyui_workspace:
